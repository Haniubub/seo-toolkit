# seo-audit — ready-to-paste posts

Short, honest posts for Reddit / X / communities. Tweak the bracket bits.

---

## Reddit — r/SEO (value-first, not a hard sell)

**A full, weighted SEO audit for ~4 cents — and it's actually good.**

I built a local audit toolkit for DeepSeek Harness. It runs entirely on your
machine: deterministic Python measurement (53 scripts) for technical SEO, on-page,
schema.org/JSON-LD, local/GBP, Core Web Vitals, GEO/AI-Overview readiness,
content/E-E-A-T, sitemap, hreflang — then LLM judgment synthesises it into one
weighted, prioritised report.

Why I think it's worth a look:
- **~$0.04 per audit.** The measurement layer is pure local Python ($0 in LLM
  tokens); only the reasoning costs anything. The same audit on Claude runs
  ~$0.45–$1.13 — 12–30x more.
- **Grounded in Google's primary sources**, not blog folklore.
- **Gated multi-agent fan-out** — it detects the industry and only runs the
  agents you need, never all 18.
- **No SaaS, no per-domain pricing**, nothing leaves your machine.

Happy to answer questions about the approach. [`seo-audit`](https://github.com/Haniubub/seo-toolkit)

---

## X / Twitter — punchy

**A complete, weighted SEO audit for ~4 cents.** Deterministic Python measurement
(53 scripts) + LLM judgment, Google-aligned scoring, runs locally, no SaaS.
12–30× cheaper than the Claude equivalent — and the quality holds up. This
quality-per-dollar gap didn't exist before. 🧵

---

## Discord / community — casual

Local SEO + technical audit toolkit for DeepSeek Harness. Drop a URL, get a full
weighted audit for ~4 cents (12–30× cheaper than running it on Claude), grounded
in Google's own guidance. No SaaS, no per-site pricing. Repo:
https://github.com/Haniubub/seo-toolkit

---

## The honest disclaimer (keep in mind)

Don't say "there's no other tool that can do this." Say: "this is the first one
I know that combines weighted + deterministic + primary-source-grounded audits at
this price." That's defensible and doesn't invite pushback.
